# To use:

# conda env create -f conf.yml
# conda activate EvolBern
name: EvolBern
dependencies:
- python=3.9
- pip
- pip:
  - numpy
  - torch
  - matplotlib
  - scipy
  - ipykernel
  - tensorboard
  - tqdm
  - pytz
